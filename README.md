[TOC]

# 数据结构

## 食用方法
- 单击右边绿色框“code”
- 下载代码（推荐git clone $(ssh)，直接下载压缩包也行）

- 在build中

  ```cmake
  cmake ..
  make
  ```

- 在主目录中

  ```
  cmake -B build -S src //反正可执行文件在build，不如直接进入build再cmake
  ```

  

